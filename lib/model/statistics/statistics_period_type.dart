/// Defines the available time periods for generating statistical reports.
///
/// Used to filter or group data based on a specified time frame.
enum StatisticsPeriodType {
  /// Represents statistics aggregated by week.
  weekStats,

  /// Represents statistics aggregated by month.
  monthStats,

  /// Represents statistics aggregated by year.
  yearStats,
}
